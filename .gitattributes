# 줄바꿈 정규화
# core.autocrlf=true 환경에서 체크아웃 시 .sh가 CRLF로 변환되면
# bash가 줄 끝 \r를 값의 일부로 취급하여 스크립트가 조용히 오작동한다.
# 디바이스와 무관하게 셸 스크립트는 LF로 고정한다.
*.sh text eol=lf

# Windows 전용 스크립트는 CRLF가 안전하므로 규칙 대상에서 제외한다.
*.cmd text eol=crlf
*.bat text eol=crlf
*.ps1 text eol=crlf
